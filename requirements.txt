# Data processing
pandas        # DataFrame operations, CSV I/O, SQL query results
numpy         # Random inventory generation and numerical operations

# Environment & configuration
python-dotenv # Loads NVIDIA_API_KEY and other secrets from .env

# Database
sqlalchemy    # SQLite engine, parameterised queries, connection management

# Agent framework
smolagents    # HuggingFace multi-agent framework — ToolCallingAgent, @tool decorator

# Structured output validation
pydantic      # Schema validation for LLM-returned JSON at every agent boundary

# Dashboard
nicegui       # NiceGUI web dashboard — run dashboard.py to view live analytics
